name: Mac Verilerini Guncelle

on:
  schedule:
    - cron: '0 * * * *'  
  workflow_dispatch:     

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
    - name: Depoyu Klonla
      uses: actions/checkout@v3

    - name: Python Kur
      uses: actions/setup-python@v4
      with:
        python-version: '3.x'

    - name: Gerekli Kütüphaneleri Yükle
      run: pip install requests beautifulsoup4

    - name: Python Botunu Çalıştır
      run: python bot.py

    - name: Yeni Verileri GitHub'a Kaydet (Commit)
      run: |
        git config --global user.name "Mac Botu"
        git config --global user.email "bot@macrehberi.com"
        git add api.json
        git commit -m "⏳ Veriler guncellendi" || exit 0
        git push
